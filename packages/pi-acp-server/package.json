{
  "name": "pi-acp-server",
  "version": "0.1.0",
  "description": "ACP JSON-RPC server wrapping pi's AgentSession, so KiroCrew can drive pi as a third backend.",
  "type": "module",
  "bin": {
    "pi-acp-server": "./bin/pi-acp-server"
  },
  "main": "./dist/server.js",
  "types": "./dist/server.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/node": "^25.5.0",
    "eslint": "^9.39.4",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "files": [
    "bin",
    "dist",
    "README.md"
  ]
}

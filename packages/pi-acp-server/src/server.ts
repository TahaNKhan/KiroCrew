// Placeholder. Real implementation lands in T08. Kept as a one-line file
// so `npx tsc --noEmit` has ≥1 input — TS raises TS18003 ("No inputs were
// found") against a fully-empty src tree regardless of tsconfig settings.
export {};
